spring:
  datasource:
    url: jdbc:mysql://localhost:3306/uploadFile
    username: root
    password: Franco
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    serialization:
      fail-on-empty-beans: false



uploadDir: ${user.dir}\src\main\resources\upload
